namespace ExeGenerator
{
    class Program
    {
        static void Main(string[] args)
        {
            var configFilePath = ConfigLoader.GetConfig("commands.yml");
            
        }
    }
}
